# Rising Fast, Falling Slow — Python Dependencies
# ─────────────────────────────────────────────────────────────
# Install with:
#   pip install -r requirements.txt
#
# IMPORTANT — geopandas is NOT listed here because pip cannot
# install it reliably on Mac/Linux due to the proj/gdal dependency.
# Install it separately via conda BEFORE running pip install:
#   conda install -c conda-forge geopandas
#
# Only required for Notebook 03 (PADD map).
# All other notebooks (01–02, 04–09) run without geopandas.
# ─────────────────────────────────────────────────────────────

# Core data
pandas>=1.5.0
numpy>=1.23.0

# API clients
fredapi>=0.5.0        # FRED API wrapper: pip install fredapi
requests>=2.28.0      # EIA API calls (standard HTTP)

# Statistical modeling
statsmodels>=0.13.0   # OLS, ECM, Wald tests, cointegration
scikit-learn>=1.1.0   # Lasso, Ridge, Random Forest, cross-validation
scipy>=1.9.0          # Statistical tests

# Machine learning
xgboost>=1.7.0        # XGBoost model (Notebook 06)
joblib>=1.2.0         # Model serialization (save/load trained models)

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0       # Correlation heatmap (Notebook 02)

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0
